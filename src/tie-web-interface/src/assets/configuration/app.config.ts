import type { AppConfiguration } from "../scripts/Application";

const config: AppConfiguration = {
    enrichment_file: "app.enrichment.json",
    trained_model: "app.trained.model.zip"
}

export default config;
